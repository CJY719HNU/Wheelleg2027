# 与参考模型(COD-2026-Balance-2.0)对齐：状态记录

> 原则：零件偏移数据与 mesh 文件名按你的保持，其余结构/写法尽量向参考看齐。
> 本文档记录每一步“做了什么 / 做到 / 做不到”，随进展更新。

## 现状文件

- `real_wheelleg_sim.xml` —— **固定基座**版（新：求解器、地面材质、轮子接触、几何估算惯量已写入）
- `real_wheelleg_sim_float.xml` —— **浮体**版（在固定版基础上给 base 加 `<freejoint/>`；其余相同）

## 已完成 ✅

| # | 项 | 说明 |
|---|---|---|
| 1 | site 式 connect | 4 个 coupler 销孔两件各放 `<site>`，connect 用 `site1/site2`；参考位形间距 0.000 mm，驱动中 0.0 mm |
| 2 | L1/L5 各自独立电机(共轴不互锁) | 与参考“前肩+后髋各一电机”同思路；并联闭环只在远端销合 |
| 3 | 求解器对齐 | `timestep=0.002 iterations=50 tolerance=1e-10 solver=Newton jacobian=dense cone=elliptic` |
| 4 | 地面+材质 | checker texture + `mat_ground`；地板 `contype/conaffinity=1 condim=3 friction` |
| 5 | 轮子能滚 | 每个轮子加了隐藏**圆柱碰撞** `size=0.068,0.03`，随轮旋转、可触地；轮轴电机保留 |
| 6 | 几何估算惯量 | 用 STL 闭合体积积分算 质量/质心/惯量张量/主轴 quat 并写入。**密度假设 ρ=2700 kg/m³(铝)**；质心/主轴由几何决定 |
| 7 | float 浮体版 | 见上；能落地有接触(实测 ncon=4、无 NaN) |

## 做到/但有限制 ⚠️

| # | 项 | 限制 |
|---|---|---|
| A | float 版“落地能站/能走” | 只有 **2 轮一线支撑、无前/后脚**，没有控制器时自由落体会倾倒（俯仰不稳）。接触、滚动本身已可用，**姿态/行走要靠控制** |
| B | 惯量精确度 | 按 ρ=2700 均匀密度估算；真实材料/空心/壁厚不同则需 CAD 实测值替换（改了质量与张量会改变动态） |

## 做不到 / 需外部数据

| # | 项 | 原因 |
|---|---|---|
| C | 与参考完全相同的零件拓扑 | 参考 4 条腿(2 前脚+2 后轮)；我们是每侧 1 条腿到轮(共 2 轮) |
| D | 精确惯量 | 需 CAD 质量属性；目前是估算 |
| E | 行走/自平衡 | 模型文件不含控制器 |
| F | mesh 名/偏移一致 | 按约定排除 |

## 备注

- 固定版 `base` 有 18.76 kg 惯量（body 体积 ~7 L×2700）。若感觉太沉/太轻，改 `_ino.json`→mass 或整体换密度重算即可（脚本见工作记录）。
- float 版如需“落地后保持姿态”，下一步可加：前/后触地缓冲、轮距或配重、或接一个简单平衡/步态控制器。
