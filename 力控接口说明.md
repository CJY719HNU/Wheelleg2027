# 关节力控（顶关节 L1/L5 与底部轮子）MuJoCo 接口说明

> 针对 `real_wheelleg_sim*.xml`：4 个顶关节（`L/R_back_drive_pos`=L1、`L/R_front_drive_pos`=L5）与 2 个轮关节（`L/R_Wheel_joint`）。
> 目标：**不用纯 PID**，直接按“力矩/力”输入，自己写控制律。

## 0. 一句话

MuJoCo **不自带控制器**。它只做两件事：你每步把 `data.ctrl` 设成你要的**力矩(N·m)/力(N)**，它做一次动力学 `mj_step`。你要的“力控” = 把顶关节从 **position(伺服) 换成 torque(motor/general)**，然后在你的循环里按力控律算 τ 写进 `ctrl`。

- 轮子目前已经是 `motor gainprm=1`：`ctrl` 单位即 N·m，可直接力控。
- 顶关节目前是 `position kp/kv`：`ctrl` 是**角度目标**，由 MuJoCo 按弹簧算力矩——这就是“PID/阻抗”式。要纯力矩力控请换成 motor（见 §2）。

## 1. 现状（`real_wheelleg_sim.xml` 等三个文件）

| 关节 | 现在的 actuator | ctrl 含义 |
|---|---|---|
| `L_J_base_L1`,`L_J_base_L5`,`R_J_base_L1`,`R_J_base_L5` | `<position ... kp=80 kv=8 ...>` | 角度目标 (rad) |
| `L_J_W`,`R_J_W` | `<motor ... gainprm=1 ...>` | 扭矩 (N·m) |

## 2. 改成“力矩输入”（力控的基础）

把顶关节的 actuator 换成 torque 型（motor 或 general）。motor 最简单：

```xml
<actuator>
  <!-- 顶关节: 纯扭矩输入, 单位 N·m -->
  <motor name="L_back_drive" joint="L_J_base_L1" gainprm="1"
         ctrllimited="true" ctrlrange="-10 10"/>
  <motor name="L_front_drive" joint="L_J_base_L5" gainprm="1" ctrlrange="-10 10"/>
  <motor name="R_back_drive" joint="R_J_base_L1" gainprm="1" ctrlrange="-10 10"/>
  <motor name="R_front_drive" joint="R_J_base_L5" gainprm="1" ctrlrange="-10 10"/>
</actuator>
```

`gainprm=1` → 电机施加广义力 = `ctrl`，即关节轴上的 **N·m**（你的腿所有铰链都绕 x 轴）。轮子同理保持 motor。

需要更复杂传动/自定义动力学的用 `general`（可配 `gear`、`lengthrange`、`gainprm/biasprm`、`user` 等），但普通力控 motor 够了。

> ⚠️ 该腿是**闭环并联(connect)**：关节力矩之间存在约束/冗余。直接给 4 个顶关节分别发任意力矩可能“打架”（约束反力顶上）。力控建议按机构整体设计（见 §5）。

## 3. Python 控制接口（你写控制律的地方）

```python
import mujoco, numpy as np
m = mujoco.MjModel.from_xml_path("real_wheelleg_sim_float.xml")
d = mujoco.MjData(m)

def joint_idx(name):            # 关节 id
    return mujoco.mj_name2id(m, mujoco.mjtObj.mjOBJ_JOINT, name)
def act_idx(name):              # 执行器 id
    return mujoco.mj_name2id(m, mujoco.mjtObj.mjOBJ_ACTUATOR, name)

J = {k: joint_idx(k) for k in ("L_J_base_L1","L_J_base_L5","R_J_base_L1","R_J_base_L5","L_J_W","R_J_W")}
A = {k: act_idx(k) for k in ("L_back_drive","L_front_drive","R_back_drive","R_front_drive","L_Wheel_joint_act","R_Wheel_joint_act")}

def controller(d):
    # —— 在这里写你的力控 ——
    tau = np.zeros(m.nu)
    q  = {k: d.qpos[m.jnt_qposadr[J[k]]]   for k in J}   # 角度
    qd = {k: d.qvel[m.jnt_dofadr[J[k]]]    for k in J}   # 角速度
    tau[A["L_back_drive"]] = ...   # 你的 L1 力矩(N·m)
    tau[A["L_Wheel_joint_act"]] = ... # 轮子力矩
    return tau

# 控制+仿真循环
while True:
    mujoco.mj_forward(m, d)          # 更新中间量(可选)
    d.ctrl[:] = controller(d)        # 写入力矩
    mujoco.mj_step(m, d)             # 前进一个 dt
    # 读回来:
    tau_applied = d.actuator_force.copy()   # 执行器真实施加的广义力(N·m), 含限幅
    q_now  = d.qpos.copy(); qd_now = d.qvel.copy()
```

### 常用读/写量

| 量 | MuJoCo | 说明 |
|---|---|---|
| 输入力矩 | `d.ctrl` | 你设的控制量；motor→N·m |
| 实际输出 | `d.actuator_force` | 经 `ctrlrange/gain/bias` 后的真实力矩，做力反馈/辨识用它 |
| 关节角 | `d.qpos[jnt_qposadr[j]]` | rad |
| 关节角速度 | `d.qvel[jnt_dofadr[j]]` | rad/s |
| 广义外力 | `d.cfrc_ext` | 作用在各 body 的等效外(接触)广义力 |
| 接触 | `d.ncon`,`d.contact`,`mujoco.mj_contactForce(m,d,i)` | 每个接触点的力 |
| 重力/偏置项 | `d.qfrc_bias`（`mj_forward` 后） | 重力+科氏+离心；qvel=0 时≈重力 |
| 执行器广义力 | `d.qfrc_actuator` | 与 `actuator_force` 相关的关节空间作用 |
| 质量阵 | `d.qM`（`mujoco.mj_fullM(m,d,M)`） | 正向动力学/计算力矩用 |
| 逆动力学 | `mujoco.mj_inverse(m,d)` → `d.qfrc_inverse` | 给“运动”求所需关节广义力 |

### 常用力控写法（示意）

- **重力补偿**：`tau = d.qfrc_bias`（静止时即重力），再叠加你要的项。
- **计算力矩/前馈+PD**：需要质量阵 `M`：
  ```python
  # τ = M*(k*(qd_des-q) + kv*(qdd_des)) + h(q,qd)
  ```
- **阻抗/导纳力控**：外环给目标力矩/刚度，`tau = K(q_des-q) - D*qd + tau_ff`——仍是你在 ctrl 里写。
- **期望关节力矩 + 关节力矩反馈**：测 `d.actuator_force`，闭环调节 ctrl 使关节力矩逼近期望（这就是“力矩控制”）。
- 轮子：直接发 `ctrl=τ_wheel`（驱动/滑移/平衡都靠它）。

## 4. 传感器（可选，把控制输入读得更方便）

在模型里给需要测量的关节加传感器，`d.sensordata` 就按序给出：

```xml
<sensor>
  <jointpos joint="L_J_base_L1"/>
  <jointvel joint="L_J_base_L1"/>
  <actuatorfrc actuator="L_back_drive"/>
</sensor>
```

类型有 `jointpos/jointvel/jointtorque`（等效力矩）、`actuatorfrc/actuatorvel/actuatorpos`、`force/torque`（外力/力矩传感器）、`framepos/framequat/framevel` 等。也可以直接在 python 里用 `m.jnt_qposadr`/`actuator_force` 读，不用传感器。

## 5. 并联闭环的注意点（重要）

- 顶关节与连杆间有 `<connect>` 闭环，关节空间存在**约束冗余**：直接独立给 4 顶关节力矩，约束会在里面分配，可能有一对力矩被约束“吃掉”或顶死。
- 可靠做法：**在关节空间设计控制但不违反闭环运动**（例如先把顶关节两两按机构联动参数化，或做整机阻抗，让约束自然满足）；或先做 **inverse dynamics / 计算力矩 + 约束一致投影**。
- 想先验证“单个关节能收到并测到力矩”：把对应关节当自由度、`ctrl` 给阶跃 τ，看 `data.qpos` 响应与 `data.actuator_force` 是否 = τ（含限幅）。

## 6. 其他

- dt = `m.opt.timestep`（当前 0.002 s）。
- 做**实时**控制仿真建议用 `mujoco.viewer.launch_passive` + 自己的 step 循环（本项目的 `land_demo.py` 就是这种骨架，把 `mj_step` 换成 `ctrl←控制器→mj_step` 即可）。
- 如果要在 MATLAB 里联调：常见桥接是 MATLAB↔Python(客户端) 通过共享内存/ZeroMQ/TCP 传 `qpos,qvel,sensordata` 和 `ctrl`；MuJoCo 侧只需把 python 控制循环里的 `controller()` 换成“收发 MATLAB 的 τ”。具体桥接库自选，MuJoCo 侧接口不变。
