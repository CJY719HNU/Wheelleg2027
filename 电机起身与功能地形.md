# 电机起身与独立功能地形

## 本次改变

删除了旧版向机体施加外力扶正的 `recovery` 实现和 `simulation_reset`。现在恢复期间只写六个 `d.ctrl`；不改变机体位置、姿态、速度或仿真时间，不施加恢复外力，也不改变重力。启动初始化和测试设置初始倒地姿态不属于恢复动作。

新实现位于 `motor_recovery.py`，由 `advanced_control.py` 的 RECOVERY 状态调用。`assist_wrench` 仅为兼容旧日志保留，始终为零，不再接入动力学。

## 启动和操作

```powershell
# 完整功能场地，接 Xbox 手柄
python advanced_control.py --gamepad --terrain course

# 单独坡道
python advanced_control.py --gamepad --terrain slope

# 单独飞坡
python advanced_control.py --gamepad --terrain launch

# 只允许手动请求起身
python advanced_control.py --gamepad --terrain course --no-auto-recovery
```

不接手柄时删去 `--gamepad` 即可用键盘。LB＋左摇杆前后/转向，LB＋A 小跳，B 急停，失能后 START 请求电机起身；键盘 E 急停、R 电机起身。

普通失稳后先失能，待接触地面且速度较低，至少等待 1 s，自动尝试一次起身。急停不会自动重启，必须明确按 START/R。失败后保持零力矩，START/R 可重试。`--no-auto-recovery` 关闭自动尝试，但不关闭手动起身。旧参数 `--physical-only` 作为此选项的兼容别名；所有运行模式现在都是电机恢复，没有仿真扶正或复位后备路径。

## 起身状态机

顶层：GROUND → DISABLED → RECOVERY → LANDING → GROUND。RECOVERY 内部按以下阶段进行：

| 阶段 | 动作 | 目的 |
|---|---|---|
| FOLD | 将腿收至约 0.10 m，保留当前腿角 | 让旋转时轮子先脱离地面，避免一开始就把机体推得更歪 |
| SWEEP | 收腿状态下连续转髋，包含完整一圈，再对准当前重力方向 | 利用髋关节连续转动能力重新布置支撑点 |
| EXTEND | 受限速度伸腿，等待轮子实际接触 | 避免悬空时直接施加最大支撑力造成关节超速 |
| PUSH | 根据接触进行大力矩 VMC 支撑，并调节机体姿态 | 用轮地接触产生的反作用力撑起机体 |
| RIGHT | 双腿协调、横滚补偿、轮端控制共同稳定姿态 | 让起身后的状态进入普通平衡控制范围 |
| SETTLE | 超速后输出全部归零，等待至少 1 s 并检查姿态与速度 | 先卸力，避免继续加速；4 s 未满足条件则失败 |
| REPACK | 翻正但腹部仍着地，或 SETTLE 满足条件后，重新进入 FOLD | 只重新规划支点，绝不重置物理状态；最多两次 |
| 交接 | 姿态、腿角、腿长和速度连续满足条件后进入 LANDING | 避免刚经过直立姿态的一瞬间就过早接管 |

髋电机峰值仍限于原模型的 ±40 N·m，轮电机仍不超过 ±4.92 N·m。没有为了“看起来能起身”修改机体质量、惯量或放大模型执行器限值。这些限值是当前模型设置，实际电机是否支持对应扭矩、转速和功率仍需硬件数据确认。

侧向倒地与前后倒地不同：两个髋轴均绕 X 方向，不能据此认定任意侧倒都可以直接翻身。当前会拒绝大横滚姿态并保持失能；具体测试结果见下方验收部分。

## 转髋为什么不能直接 wrap 位置误差

普通角度误差用 `wrap(q_des-q)` 会选择最短转动路径，使“转一整圈”的目标与“不转”相同。因此：

1. 腿角指令连续积分，SWEEP 参考值不折回 ±π。
2. 逆运动学求出髋关节角后，提升到与上一帧一致的圈数：

   `q_des += 2*pi*round((q_des_prev-q_des)/(2*pi))`

3. 收腿与摆腿阶段使用真实连续关节误差：

   `tau = clip(180*(q_des-q) - 4*qdot, -40, 40)`

这里的姿态三角函数仍然周期化，但关节伺服误差不做最短角折回。正转沿原 XML 的局部 -X 轴满足右手定则，L1 对应 phi4，L5 对应 phi1。

## 支撑、协调与防打滑

撑起阶段使用原闭环雅可比：

$$\tau_{hip}=J^T[F,T_p]^T.$$

径向支撑近似为：

$$F=\operatorname{clip}(F_{eq}/2+2200(h_d-h)-90\dot h,-120,300).$$

大俯仰时目标长度为 0.34 m，接近直立后降为 0.30 m。允许有限的负径向力用于回收过度伸展的腿，避免只能向外推、不能主动收回。

角力矩参考原 LQR 输出，增加左右反号的腿角同步项和径向横滚补偿。恢复过程中的轮速观测受限，不允许打滑读数无限放大恢复驱动力；轮端另加速度阻尼，驱动力矩受接触载荷预算约束。

当前轮端预算按 `min(4.92, 0.65*F_contact*R_wheel)` 限制，悬空轮为零力矩。接触载荷来自 MuJoCo 接触反馈，未来实机需要替换为可靠的支持力估计或传感器反馈。

这套闭环修正比“给四个髋关节同时打一段最大力矩”更可控，但大姿态恢复依然是非线性接触问题，不能由站立 LQR 的局部稳定性保证。

## 保护和交接

- 总恢复时间超过 24 s、严重横滚（超过 0.75 rad）、闭环误差超过 6 mm 或重新布置次数超限时退出到 DISABLED。髋超过 22 rad/s 或轮超过 65 rad/s 时先进入 SETTLE 零输出等待；等待失败则失能。
- 接管要求两轮接触、俯仰/横滚接近直立、两腿回到设计范围内、腿角接近平衡角、腿长和机体角速度较小，并持续满足一段时间。
- 交接只重置控制器的滤波器/参考状态；速度估计从接地轮速观测初始化。物理 qpos/qvel 不修改。
- 失败后不调用任何“把机器人放回去”的函数。

## 地形如何独立加载

地形配置在 `terrains/`，由 `terrain_scene.py` 在编译模型时加入 worldbody。基础机器人 XML 不含这些场地对象，换场地无需改连杆、质量、惯量或驱动器。

| 配置 | 内容 |
|---|---|
| `slope.json` | 1.2 m 长上坡、高 0.10 m 平台、下坡 |
| `launch.json` | 0.4 m 长、高 0.20 m 的起跳坡；坡端落差和落地区 |
| `course.json` | 中间坡道、右侧飞坡、左侧 3 cm/6 cm 台阶 |

坡面用凸三棱柱碰撞几何生成，坡底与地面齐平。平台/台阶是 box。它们是真实静态碰撞体，轮子和机体可以与之接触，不是显示图片。底层地面仍是无限平面；视觉范围扩大到能看到整个场地。

`course` 中沿车头 +Y 方向前进：坡道通道 x=0，飞坡通道 x=+1.5 m，台阶通道 x=-1.5 m。用方向控制驶入相应通道。

自定义配置示例：

```json
{
  "objects": [
    {"name":"my_ramp", "type":"ramp", "pos":[0,1,0], "size":[1.2,0.8,0.06]},
    {"name":"my_platform", "type":"box", "pos":[0,2.1,0.03], "size":[1.2,0.6,0.06]}
  ]
}
```

`size` 单位 m，顺序是完整宽度、纵向长度、高度。ramp 的 `pos` 是坡道起点底面中心，沿 +Y 上升；加 `descending:true` 可生成下坡。box 的 `pos` 是几何中心，加载器会把完整尺寸转换成 MuJoCo 的半尺寸。

```powershell
python advanced_control.py --terrain-file terrains/my_course.json
```

当前飞坡高 20 cm、长 40 cm；2 m/s 助跑通行尚未稳定通过。此前 6 cm 飞坡的成功记录已归档，不能外推到当前场地。测试及失败边界见 [当前验收](速度跳跃与飞坡验收.md)。

## 模型边界

当前验证证明的是：在现有 MuJoCo 模型中，只用执行器与接触动力学完成动作。模型仍使用原来的机体碰撞盒，连杆之间/连杆与机体的完整自碰撞没有建立；整圈转髋是否会与真实机体、线束或关节机械限位干涉，尚不能由此模型证明。后续应先完成 CAD 间隙与自碰撞检查，再把它当作实机起身轨迹。

## 验收结果

具体数据见 [当前验收](速度跳跃与飞坡验收.md)，本次重测了塌腿、倒置起身；其余姿态的历史测试不代表当前版本重新验收。恢复测试在 t=0 设置倒地初始条件，之后每一步都比较控制调用前后的 qpos/qvel/time，验证控制器没有修改它们，并检查无外加恢复力。
