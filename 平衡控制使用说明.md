# 轮腿基础平衡、VMC 与变腿高控制

## 启动

在项目目录运行：

```powershell
python balance_control.py
```

启动时会先解算闭环并将浮体放在双轮接地的平衡附近，然后持续执行力矩控制。无需 MATLAB 在线运行，也不会触地自动暂停。旧 `land_demo.py` 是无平衡控制的落地演示，请使用新入口。

| 操作 | 功能 |
|---|---|
| W / S | 目标等效腿长增加 / 减少 0.01 m |
| 上 / 下方向键 | 前进速度指令增加 / 减少 0.025 m/s，限于 ±0.2 m/s |
| 空格 | 将速度指令置零，继续平衡 |
| R | 重新放到平衡初态，清零速度和积分状态 |
| 关闭窗口 | 退出 |

腿长指令范围为 **0.27–0.35 m**，变化速度限制为 **0.015 m/s**。窗口叠加显示左右实测腿长、目标腿长和机体俯仰角。

脚本不负责从倒地状态自行站起，也不是高空落地恢复控制器；超出俯仰/横滚约 37°的基础恢复范围会停止并报错。重新运行可复位。此版本针对平地双轮接触的基础平衡。

## 交付文件

- `balance_control.py`：控制入口、闭环正逆运动学、VMC、LQR 增益设计和调度、变腿长、横滚及左右腿差动稳定。
- `real_wheelleg_balance.xml`：新增浮体力矩模型，六个 actuator 全部为 `motor gear=1`。保留四个 site-connect、几何估计惯量、机体/轮子碰撞和明亮灯光。
- `balance_gains.json`：9 个腿长点的 A、B、K、静态平衡输入和闭环谱半径。
- `balance_design.mat`：同一设计数据的 MATLAB 文件。
- `verify_balance.py`：左右编码器正运动学、雅可比有限差分、虚功关系和渲染校验。
- `verify_balance_envelope.py`：腿长上下边界及低速前进/停车校验。
- `balance_*_log.json` / `balance_*_log.csv`：站立、变腿长和扰动实验的数据。
- `平衡控制验收.md`：真实测试结果与验收范围。

没有覆盖此前三个 `real_wheelleg_sim*.xml` 模型或 `test.m`。

## 坐标、phi1、phi4 和输入方向

仿真世界系：X 为轮轴方向，**+Y 为前进，+Z 向上**。所有腿和轮子的 hinge 轴均为 `[-1,0,0]`，正 q、正角速度、正力矩都按 **-X 轴的右手螺旋定则**。

运动学侧视平面取 `u=+Y`、`v=-Z`，角度由水平 +u 朝 +v 增大，因此与 -X 轴的右手定则一致。这里是在机体参考系中定义杆件角度。

```text
phi4 = atan2(0.062224,  0.095167) + q_L1
     = 33.178268° + q_L1（q_L1 为弧度时先将常数换成弧度）
phi1 = atan2(0.125442, -0.168417) + q_L5
     = 143.320161° + q_L5
```

所以参考装配姿态中 L1 是锐角 phi4，L5 是钝角 phi1。实际运动中保持连续角度，不强行把角度折回锐角或钝角。右腿采用同样公式，因为 XML 的轴也是 -X，不能再凭左右镜像额外取反。

`Robot.phi_angles(data, side)` 返回 `[phi1, phi4]`，单位 rad；`side` 为 `L` 或 `R`。

`Robot.forward_leg(q_L1, q_L5, side)` 只用两个主动关节的相对角度，通过两个闭环解出四个被动关节，返回腿长、相对腿角和 VMC 雅可比。它在当前装配分支附近解算；不能跨机械奇异点跳到另一个装配分支。

### 腿角和六状态

- `alpha`：等效腿相对机体的倾角，朝前倾为正。
- `theta`：轮轴指向髋轴的连线相对世界竖直方向的倾角，髋轴在轮轴前方为正。
- `pitch`：机体绕 -X 的俯仰角，上部朝 +Y 倾为正。
- 在平面运动中 `theta = alpha + pitch`。
- `phi0 = pi/2 + alpha`，因此 `d(phi0)/dq = d(alpha)/dq`。

严格按 `test.m` 的顺序使用：

```text
state = [theta, theta_dot, hip_y, hip_y_dot, pitch, pitch_dot]
input = [T, Tp]
Q = diag([1, 1, 500, 100, 5000, 1])
R = diag([1, 0.25])
```

这里 T、Tp 是**整机左右合计的虚拟输入**，分配到每侧时除以 2。不能把单侧输入幅值未经换算地套入此 K。

注意：这里 `hip_y` 对应文档的 `x_b`（髋轴平移），不是世界 X，也不是轮子位移。轮子绝对滚动角包含机体、L5 和 L6 的转角；离线建模使用纯滚动约束，未把轮子相对编码器角当作绝对转角。

## LQR 实现与 test.m 的关系

启动时读取 `test.m` 的 Q_cost/R_cost，数值不作重新调权。采用零阶保持离散化，同时积分连续二次代价中的 Q/R/交叉项，再求离散 Riccati 方程，采样周期 **0.001 s**，对应 MATLAB `lqrd` 的设计方式。

`test.m` 中质量、半径等仍标注为借用其他机器人的值，不能直接用于本机。新实现读取当前 MuJoCo 模型的质量矩阵、重力偏置、阻尼及闭环几何，构造满足闭环和轮子纯滚动的降阶坐标：

```text
z = [theta, hip_y, pitch, L0]
q_full = f(z)
qvel_full = E(z) * zdot
M_reduced = E' * M_full * E
G_reduced = E' * qfrc_bias(q,0)
```

每个固定腿长处求解静态平衡点，再线性化前 3 个坐标，得到六状态、两输入的 A/B。机体质心不在髋轴正上方，平衡 theta 和 Tp 因此不为零。反馈是 `u = u_trim + K * (state_ref - state)`。

0.27–0.35 m 每 0.01 m 建一个表项，运行时线性插值 K 和静态输入。没有照搬五杆长度、论文中某一台机器人的现成 K，也没有把未验证的二次多项式外推到工作区外。真实模型仍为完整浮体六连杆仿真。

更改 Q/R 或 `real_wheelleg_balance.xml` 后运行：

```powershell
python balance_control.py --design --headless --scenario stand --seconds 20
```

缓存记录 XML 的 SHA-256、控制周期和 Q/R，模型或权重变化后旧缓存会被拒绝。

## VMC：适配当前六连杆，不套用五杆公式

每侧主动关节序为 `[q_L1, q_L5]`，被动关节为 L1–L2、L5–L3、L3–L4、L5–L6。

对两个闭环在运动平面内求速度约束：

```text
C_active * dq_active + C_passive * dq_passive = 0
S = [I; -solve(C_passive, C_active)]（按实际关节顺序排列）
J = d[L0, alpha]/dq_full * S
[tau_L1; tau_L5] = J' * [F_extension; Tp_side]
```

两侧各独立计算一次，使用完整六杆闭环的雅可比。若需要论文的 `[phi1,phi4] = [L5,L1]` 顺序，将返回 J 的两列交换，即 `J_paper = J[:, [1,0]]`。

正的 F_extension 使腿伸长。Tp 使 alpha 增大，同时对机体施加反向力矩；这是内部关节力矩，不是凭空给机体加外力。运行时仅写六个 actuator 的 `ctrl`，不会改 qpos 或用虚拟外力托住机身。只有实验的 push 场景会短时向机体加外力。

## 变腿长和垂直腿高

文档和界面默认控制的是等效腿长 `L0`，即髋轴到轮轴在机体运动平面内的距离。垂直腿高另行记录为 `vertical_height`；两者在平面内满足 `H = L0*cos(theta)`，均不含轮子半径。

每侧使用几何重力前馈、径向弹簧/阻尼和限幅积分：

```text
F = F_trim/2 + 1800*(L_ref-L) - 65*L_dot + 150*integral(L_ref-L)
```

积分限制 ±0.08 m·s，轴向力限制 0–400 N。该径向柔顺环之外，基础平衡仍是 LQR，不是用 PID 替代平衡控制。左右腿倾角差另有差动反馈，横滚误差以差动支撑力调节，偏航误差以差动轮矩抑制。

程序接口：

```python
controller.target_height = 0.33  # 设置等效腿长目标，名称兼容窗口的 height 参数
controller.set_vertical_height(0.30)  # 按静态倾角调度换算为垂直腿高 0.30m 的目标
controller.velocity = 0.10  # 平地 +Y 速度参考，m/s
```

垂直腿高接口在平衡附近换算，不能理解为越障地形下的独立地面高度估计器；超出对应可达区间会报错。

## 六个力矩接口

| ctrl 索引 | actuator | joint | 限幅 N·m |
|---|---|---|---|
| 0 | L_L1_torque | L_J_base_L1 | ±40 |
| 1 | L_L5_torque | L_J_base_L5 | ±40 |
| 2 | L_W_torque | L_J_W | ±4.92 |
| 3 | R_L1_torque | R_J_base_L1 | ±40 |
| 4 | R_L5_torque | R_J_base_L5 | ±40 |
| 5 | R_W_torque | R_J_W | ±4.92 |

优先按 actuator 名查 ID，不在外部控制程序里依赖此索引表永久不变。髋关节 ±40 N·m 是本次仿真执行器限制，尚无你的真实电机/减速器参数来确认硬件能力；0.30m 站立时每个髋电机需要约 13–15 N·m。

```python
import mujoco
robot = Robot()
data = robot.d
# 读取角度、速度（不要把 qpos 地址当 qvel 地址，浮体长度不同）
jid = robot.m.joint('L_J_base_L1').id
q = data.qpos[robot.m.jnt_qposadr[jid]]
qd = data.qvel[robot.m.jnt_dofadr[jid]]
# 替换控制器时使用：
data.ctrl[robot.m.actuator('L_L1_torque').id] = 5.0
mujoco.mj_step(robot.m, data)
```

此前力控说明有几处需要纠正：`motor` 用 `gear="1"`，不要给 motor 写 `gainprm`；当前本机 `mj_fullM` 签名为 `mj_fullM(model, M_dense, data.qM)`。`qfrc_bias` 含重力和速度偏置，不能把浮体全部广义力直接写给六个电机。`actuator_force` 是执行器空间力，在本模型 gear=1 时才可直接作为相应关节力矩。

以前质量惯量 MD 的旋转公式方向也有误：MuJoCo inertial quat 表示惯量主轴系到 body 系，因此应为 `I_body = R*diag(I_principal)*R'`。此控制器直接使用编译后模型的质量矩阵，没有读取那份 MD 中错误转换的矩阵。

## MATLAB 查看

```matlab
S = load('balance_design.mat');
% S.lengths: N×1；S.A: 6×6×N；S.B: 6×2×N；S.K: 2×6×N
% S.trim: N×4，列为 theta_eq, T_eq, Tp_eq, F_eq
k = 4; % 0.30m
K_check = lqrd(S.A(:,:,k), S.B(:,:,k), S.Q, S.R, S.dt);
norm(K_check-S.K(:,:,k), 'fro')

T = readtable('balance_height_log.csv');
plot(T.time, [T.length_L,T.length_R,T.target_length]);
xlabel('time / s'); ylabel('equivalent leg length / m');
legend('L','R','reference'); grid on;
```

MATLAB 的 `K_check` 比对代码已提供，但本机没有调用 MATLAB 执行；Python 的离散设计与非线性仿真已经实际执行。

## 已知范围

- 当前仿真状态读取使用 MuJoCo 位姿与速度、并对差分速度低通；没有宣称已接入真实 IMU、卡尔曼融合或 CAN 电机协议。`forward_leg` 提供双编码器运动学基础，真实机器还需传感器标定和状态估计。
- 所有惯量沿用几何估计值，总质量 22.2186 kg；这不是称重或 CAD 材料辨识结果。
- 验证针对平地、双轮接触、0.27–0.35m、当前碰撞与摩擦参数。跳跃、楼梯、打滑、自起身和大角度倒地恢复未实现。
- GUI 渲染已通过离屏预览；用户实际鼠标/键盘交互未由自动测试代操作。
- 当前 Python 实现以可验证为目标，闭环线性代数和离线数值设计未面向 MCU 做实时优化。

## 依据

- 本地 `轮腿机器人控制.md` 第 3、4 节：六状态 LQR、VMC、变腿长及左右差动控制。
- `test.m`：Q/R 权重、1ms 离散设计思路；不照用其中外部机器人的质量/半径。
- `轮腿式平衡机器人控制_陈阳.pdf` 第 4–5 页：图 4、1.2.2 VMC、虚功映射；第 6 页：相同的 Q/R。
- MuJoCo 官方 XML 说明：https://mujoco.readthedocs.io/en/3.4.0/XMLreference.html#equality-connect 。旧模型曾把 body-local anchor 当成 world anchor，这是之前闭环异常分析中的错误；不能把旧错误残差直接解释成“并联腿必须同相”。正确 site 约束允许在可达区域内两个独立髋输入改变姿态与腿长。
