# 坡道、加速度与离地增益修改教程

本次没有改动坡道尺寸或默认加速度，方便你自己调。新增 `--accel` 参数，默认仍为 0.6 m/s²；离地反馈改写为显式增益屏蔽，保持原有数值行为。

## 1. 修改坡道模型

坡道由 `terrain_scene.py` 根据 JSON 生成，**不用 Blender，也不用修改机器人 XML/STL**。

| 启动参数 | 读取文件 |
|---|---|
| `--terrain launch` | `terrains/launch.json` |
| `--terrain slope` | `terrains/slope.json` |
| `--terrain course` | `terrains/course.json` |
| `--terrain-file terrains/launch_custom.json` | 指定的独立配置 |

三个预设文件彼此独立。只修改 `launch.json` 不会改变 `course.json` 里的飞坡。

### 第一步：找到坡道对象

打开 `terrains/launch.json`，找到 `"name": "launch"` 的对象。关键内容如下，原文件的换行形式不同也没关系：

```json
{
  "name": "launch",
  "type": "ramp",
  "pos": [0, 1, 0],
  "size": [1.2, 0.4, 0.2],
  "rgba": ".75 .4 .15 1"
}
```

- `size`：**[完整宽度、水平投影长度、高度]**，单位都是米。这里不是 MuJoCo XML 中 box 的半尺寸。
- `pos`：坡道起点底面中心的 **[x, y, z]**。当前从 y=1 开始，到 y=1.4 结束，沿 +Y 上升。
- `rgba`：显示颜色，不决定碰撞尺寸。
- `descending: true`：同样的尺寸从高端向低端下降。

### 第二步：增大长度，让坡变缓

坡角由下面的关系决定：

$$\beta=\arctan(H/L),\qquad L=H/\tan\beta.$$

保持高 0.20 m 不变，可以这样比较：

| 水平长度 L | 高度 H | 坡角 β |
|---:|---:|---:|
| 0.40 m（当前） | 0.20 m | 26.57° |
| 0.80 m | 0.20 m | 14.04° |
| 1.20 m | 0.20 m | 9.46° |
| 2.00 m | 0.20 m | 5.71° |

例如改为长 1.20 m、高 0.20 m：

```json
"size": [1.2, 1.2, 0.2]
```

若起点仍为 y=1，终点就变成 y=2.2。长度改变后，若还连接平台/下坡，应一起移动后续对象，避免缝隙或重叠。坡端直接落到地面的飞坡不需要添加平台。`landing_pad` 是当前与地面齐平的透明落地区盒，不决定坡角。

也可以复制 `launch.json` 为 `launch_custom.json` 后修改，保留当前预设。

### 第三步：保存并重新启动

先关闭旧仿真窗口，再运行：

```powershell
python advanced_control.py --gamepad --terrain launch --start-y -7
# 或使用复制后的自定义文件
python advanced_control.py --gamepad --terrain-file terrains/launch_custom.json --start-y -7
```

`--start-y -7` 仅在启动时把机器人初始位置设在后方，给原本从 y=1 开始的坡道留出约 8 m 助跑空间；不是运行中移动机器人。配置不会热更新，保存 JSON 后必须重启。

如果提示 JSON 解析错误，检查逗号、双引号、括号；JSON 不支持 `//` 注释。若只是想确认模型能加载，可用 `--headless --seconds 0.1`，但这不等于验证能通过坡道。

现有 `verify_terrain.py` 包含针对预设尺寸的固定射线采样点，改尺寸后其预期高度也要相应调整。坡面在位置 y 的理论高度为 `z_start + H*(y-y_start)/L`，适用于坡道范围内的上坡。

## 2. 修改加速度

最高速度与加速度是两个参数。当前摇杆最大速度是 2 m/s，但参考速度每秒最多变化 0.6 m/s，所以从 0 到 2 的参考爬升约需 3.33 s。

### 方法 A：启动参数（不必改代码）

```powershell
# 把参考加速度改成 1.2 m/s²
python advanced_control.py --gamepad --accel 1.2
# 与地形参数一起使用
python advanced_control.py --gamepad --terrain launch --start-y -7 --accel 1.2
```

| `--accel` | 参考速度 0 → 2 m/s 的时间 |
|---:|---:|
| 0.6 | 约 3.33 s |
| 1.2 | 约 1.67 s |
| 2.0 | 约 1.00 s |

这些是**参考指令**的爬升时间，实际加速度还受轮端力矩、抓地力和姿态控制影响；不是保证车体在该时间内精确达到 2 m/s。1.2 的参考斜率经过程序检查，但更快加速的完整坡道通行未验收。

### 方法 B：改默认值

在 `advanced_control.py` 搜索：

```python
p.add_argument('--accel', type=float, default=.6, ...)
```

将 `default=.6` 改为希望的值，并同步更新帮助文字。若直接在自己的脚本中构造 `HybridController`，设置：

```python
c.drive_accel = 1.2
```

真正生效的速度更新相当于：

```python
self.speed += np.clip(target_speed - self.speed,
                      -self.drive_accel * DT,
                       self.drive_accel * DT)
```

即 $v_{ref,k+1}=v_{ref,k}+\operatorname{clip}(v_{cmd}-v_{ref,k},-a\Delta t,a\Delta t)$。

该参数同时控制普通加速和减速。松开 LB、指令超时、普通停车会按此斜率降低参考速度；B/E 急停仍直接失能，不经过缓慢减速。转向参考有独立的 0.8 rad/s² 斜率，不受 `--accel` 影响。

先在平地确认加速与停车，再测坡道；出现打滑或力矩饱和时，继续增大参考加速度不一定能使车更快。不要为了达到指令速度直接删除失能保护。

## 3. 论文到底保留了哪些离地增益

补充：连续力矩饱和的失能等待时间已由 0.3 s 放宽至 0.8 s，在 `HybridController` 中修改 `self.torque_saturation_timeout`。这只降低短时顶到力矩上限时的触发灵敏度，髋 ±40 N·m、轮 ±4.92 N·m 输出限幅不变；倾角、腿长和超速保护独立生效。界面若显示 `leg envelope`，代表腿长/运动学保护，并非力矩饱和保护。

核对本地《轮腿式平衡机器人控制_陈阳.pdf》：

- PDF 第 2 页、期刊页 649，表 1：θ 是摆杆/虚拟腿与竖直方向夹角；φ 是机体与水平方向夹角。
- PDF 第 4 页、期刊页 651：状态顺序为 `[θ, θ_dot, x_b, x_b_dot, φ, φ_dot]`，输出为轮力矩 T、虚拟髋力矩 Tp。
- PDF 第 11 页、期刊页 658：离地时保留 **K₂₁、K₂₂**，其余反馈增益置零，目标是保持腿部竖直。

所以论文的意思是保留 **虚拟腿倾角及其角速度**，不是只保留机体倾角。当前代码中机体俯仰是 `pitch`，处于第 5 列，不能把它和 θ 混用。

$$
K_{air}=\begin{bmatrix}
0&0&0&0&0&0\\
K_{21}&K_{22}&0&0&0&0
\end{bmatrix},\qquad
u_{air}=-K_{air}x.
$$

这给出 `T=0`，`Tp=−K21*theta−K22*theta_dot`。目标 θ=0 表示虚拟腿竖直，角速度目标为 0。

### 当前代码如何实现

`advanced_control.py` 新增 `airborne_lqr`：

```python
K_air = np.zeros_like(K)
K_air[1, :2] = K[1, :2]
return -(K_air @ state)
```

Python 下标从 0 开始，所以 `[1,0]` 是论文的 K₂₁，`[1,1]` 是 K₂₂。创建新矩阵不会破坏接地时的完整 K；落地后自然重新使用完整反馈，不会因为原地清零而丢失增益。

原代码的 `-.5*(K[1,0]*theta+K[1,1]*td)` 已经是这一公式的一半，分给每条腿。本次改为显式矩阵便于核查，**没有把机体倾角增益误加进论文离地控制**，也没有添加静态站立力矩前馈。

LQR 屏蔽不等于把所有控制环节关掉：当前还保留腿长弹簧阻尼、双腿同步与执行器限幅。这些是独立于六状态 LQR 的工程控制。单轮离地时只对悬空侧使用这一策略；接地侧仍需要支撑和平衡。双轮离地时两轮输出都是零。

### 能否同时保证机体水平和腿竖直

这项策略直接控制的是 θ 和 θ_dot，并不直接反馈 φ。它利用髋力矩调整腿部姿态，机体也会受到反作用；在有限腾空时间、已有角动量和扭矩限制下，不能独立保证机体与腿同时严格达到两个姿态目标。

只保留机体倾角列则会变成另一种控制器：它不再直接控制腿部竖直，不能称为论文的 K₂₁/K₂₂ 策略。若目标是更强的机体空中姿态控制，应单独建立腾空动力学、处理角动量与接触切换，再验证，不宜只换两列增益就作保证。

本次用 `verify_airborne_feedback.py` 检查：改变位置、速度、机体倾角/角速度不会改变离地 LQR 输出；轮输出为零；接地 K 不被修改；加速度参数确实改变参考速度斜率。另重跑深蹲跳跃检查实际落地结果。
